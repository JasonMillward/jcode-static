ci:
  collect:
    numberOfRuns: 5
    url:
      - https://jcode.me/
#  assert:
#    preset: lighthouse:recommended
  
  upload:
    target: lhci
    githubToken: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
    token: ${{ secrets.LHCI_TOKEN }}
    serverBaseUrl: ${{ secrets.LHCI_URL }}
