ci:
  collect:
    numberOfRuns: 5
    startServerCommand: yarn serve
    startServerReadyPattern: "You can now view"
    startServerReadyTimeout: 60000
    url:
      -  http://localhost:8000/

  upload:
    target: lhci
